# 持续改进

一次改进解决一个真实、可描述的问题。优先修改已有 owner，保留可工作的现用版。

```powershell
git switch -c improve/specific-problem
# 修改对应 skill 与必要案例
python scripts/harness.py validate
python -m unittest discover -s tests -v
git diff --check
git diff
```

## 判断问题来源

| 来源 | 处理 |
|---|---|
| 规则错误或缺失 | 对应 owner 的最小候选修改 |
| 规则正确但执行未遵守 | 记录回归案例，先不要扩充规则 |
| 工具、路径或环境失败 | 修复实际环境或明确该能力的依赖 |
| 某篇论文/项目独有要求 | 留在项目上下文 |

沿用 `skills/skill_evolve_workflow/SKILL.md` 中现有的行为变更评估要求。
Git 分支承担候选版本隔离，不把候选覆盖到唯一现用副本。

## 评估三层分开报告

1. **结构与工具检查**：路径、技能元信息、Python/JSON、确定性函数测试。
2. **任务行为**：在相同输入和约束下实际调用，检查 owner、权限、执行与交付。
3. **科研效果**：真实章节/分析任务的证据支撑、科学表述与作者修改负担。

现有 31 条 regression 是用例定义，`validate` 不调用模型、不自动评分。
其中 2 条私有质量案例在论文片段缺失时必须标为未执行；不以虚构案例替代。
运行真实模型前明确任务和成本范围；科研效果须由作者评估。保留重要负面结果。

可用 `templates/change_review.md` 记录一次改进，无需给每次任务建立新状态体系。
真实论文、输出全文、个人笔记放入本地忽略目录或研究项目中。上传 GitHub 的
案例应经脱敏，并明确合成/公开/私有来源和实际执行状态。

## 合并与回滚

检查 diff、针对性验证和作者反馈后合并；再在明确的安装范围内更新现用技能。
源码可用 `git revert <commit>` 撤回，安装则恢复该次更新前保存的受影响文件。
源码合并与本机安装分别确认，避免 GitHub 与实际使用的版本悄悄分叉。

首版不配置 CI、自动同步、自我更新或模型评分服务。现有验证命令可以日后接入
CI；是否值得接入取决于实际改动频率与维护需求。
